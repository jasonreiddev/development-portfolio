import styled from 'styled-components';

/* styled as sub without using the sub tag*/
export const Text = styled.span`
  bottom: -0.25em;
  font-size: 75%;
  line-height: 0;
  position: relative;
`;

export const TextStyles = {
  Text,
};
